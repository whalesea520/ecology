IF NOT EXISTS (
	SELECT
		*
	FROM
		sysobjects
	WHERE
		name = 'CRM_FAILFACTOR'
	AND type = 'U'
) CREATE TABLE CRM_FAILFACTOR (
	ID INT PRIMARY KEY,
	FULLNAME VARCHAR (150),
	DESCRIPTION VARCHAR (150),
	ORDERKEY INT
) 
GO