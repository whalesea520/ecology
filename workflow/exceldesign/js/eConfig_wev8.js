/**
 * 是否启用单元格自动扩展为多内容 
 * true:自动扩展；false:替换单元格不自动扩展
 */
var isAutoExtendMoreContent = true;

/**
 * 是否在扩展时自动打开多内容面板，只有当isAutoExtendMoreContent为true此设置才有效
 * true:打开； false:不打开;
 */
var extendOpenMcPanel = true;